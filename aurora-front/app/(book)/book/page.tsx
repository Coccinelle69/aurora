import { HeroBookingSection } from "@/components/";

const BookingPage = () => {
  return (
    <main className="relative">
      <HeroBookingSection />
    </main>
  );
};

export default BookingPage;
